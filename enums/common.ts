export enum ModalType {
    CREATE = 'create',
    EDIT = 'edit',
    DELETE = 'delete',
}

export enum OrderDirection {
    ASC = 'ASC',
    DESC = 'DESC',
}

export enum PageSize {
    SMALL = 10,
    MEDIUM = 20,
    LARGE = 50,
}
